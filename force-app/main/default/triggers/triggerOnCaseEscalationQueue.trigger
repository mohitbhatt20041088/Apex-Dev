trigger triggerOnCaseEscalationQueue on Case (after update) {
    // if (Trigger.isUpdate && Trigger.isAfter) {

    //     for(Case oldCase : Trigger.old){
    //         CaseManagmentHandler.afterUpdate(oldCase, Trigger.newMap.get(oldCase.Id));
    //     }

    // }
}   